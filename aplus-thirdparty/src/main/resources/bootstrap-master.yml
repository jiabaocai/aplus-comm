server:
  port: 10010
#服务名字
spring:
  application:
    name: aplus-thirdparty
  cloud:
    config:
      discovery:
        enabled: true
        service-id: news-config-server  # 注册中心的服务名
      profile: master  # 指定配置文件的环境
