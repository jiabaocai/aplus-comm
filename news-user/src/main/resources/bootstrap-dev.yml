server:
  port: 8080
#服务名字
spring:
  application:
    name: news-user
  cloud:
    config:
      discovery:
        enabled: true
        service-id: news-config-server  # 注册中心的服务名
      profile: dev  # 指定配置文件的环境
